# 当ディレクトリの説明
このディレクトリは別の外部サーバで動作させます。

## 理由
学内ネットワークではメールサーバのポートが閉じられているため、メール送信ができません。そのため、学外のサーバを利用してメール送信を行う必要があります。  
また、同じPCを使用してollamaを動作させる場合、高負荷によりollamaの動作が不安定になる可能性があるため、別のサーバで動作させることにしました。